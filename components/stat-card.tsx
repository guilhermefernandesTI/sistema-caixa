import { ArrowDownRight, ArrowUpRight, LucideIcon } from "lucide-react";

export function StatCard({ label, value, change, trend, icon: Icon, tone = "light" }: { label: string; value: string; change?: string; trend?: "up" | "down"; icon: LucideIcon; tone?: "light" | "dark" }) {
  return <div className={`rounded-2xl p-5 shadow-soft ${tone === "dark" ? "bg-[#15231f] text-white" : "border border-[#e4e8df] bg-white"}`}><div className="flex items-start justify-between"><div><p className={`text-xs font-medium ${tone === "dark" ? "text-white/55" : "text-ink/50"}`}>{label}</p><p className="mt-3 text-2xl font-bold tracking-tight">{value}</p></div><div className={`grid h-10 w-10 place-items-center rounded-xl ${tone === "dark" ? "bg-mint text-ink" : "bg-[#eef6e8] text-forest"}`}><Icon size={19} /></div></div>{change && <div className={`mt-4 flex items-center gap-1 text-xs font-semibold ${trend === "down" ? "text-[#d97757]" : tone === "dark" ? "text-mint" : "text-forest"}`}>{trend === "down" ? <ArrowDownRight size={14} /> : <ArrowUpRight size={14} />}{change}<span className={`font-normal ${tone === "dark" ? "text-white/40" : "text-ink/40"}`}>vs. mês anterior</span></div>}</div>;
}
